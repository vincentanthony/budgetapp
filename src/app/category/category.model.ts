export class Category {
  public number: number;
  public description: string;
  public total: number;

  constructor(number: number, description: string, total: number) {
    this.number = number;
    this.description = description;
    this.total = total;

  }
}
